import * as firebase from "firebase-admin";

export const id = "migrating-user-information-from-firestore-to-firebase"
export const description = "migrating user information from firestore to firebase"

export function up(admin: firebase.firestore.Firestore): void {
}

export function down(admin: firebase.firestore.Firestore): void {
}